# Статус

Задача завершена и принята 25 сентября 2026 года.

## 1. Планирование

- Статус: `принят`.
- Сделано: требования формы входа и визуальный референс зафиксированы в плане.
- Осталось: —
- Проверка: границы задачи и критерии приёмки определены.

## 2. Реализация

- Статус: `принят`.
- Сделано: добавлена форма с `idInstance`, `apiTokenInstance` и предзаполненным `apiUrl`; реализованы валидация, переключение видимости токена и сохранение параметров в Redux state.
- Сделано: добавлена авторизованная оболочка с левой панелью, кнопкой «Выход» и панелью «Чаты» с поиском по номеру телефона.
- Сделано: вход и рабочая область используют единый градиент и локальный фоновый узор сайта-примера.
- Принятый результат: заполненная корректными значениями форма переводит пользователя в авторизованное состояние; выход очищает сессию и возвращает форму входа.
- Осталось: —
- Проверка: авторизованная оболочка отображается после входа; кнопка «Выход» очищает state и возвращает форму авторизации.

## 3. Проверка

- Статус: `принят`.
- Сделано: проверены production-сборка, ESLint, Prettier, валидация полей, вход, авторизованная оболочка и выход.
- Осталось: —
- Проверка: `npm run build`, `npm run lint`, `npm run format:check` и `git diff --check` проходят; ручной smoke test подтвердил предзаполненный URL, блокировку входа при неверном URL, сохранение трёх параметров, отображение панелей и возврат к форме после выхода.
